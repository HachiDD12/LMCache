# Core dependencies for the blend server
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# ML/AI dependencies
transformers>=4.30.0
vllm>=0.2.0

# HTTP client for testing
requests>=2.28.0

# LMCache (if available)
# Note: LMCache may need to be installed separately or from source
# lmcache>=0.1.0

# Optional: for better performance
# torch>=2.0.0
# accelerate>=0.20.0 